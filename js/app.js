const $=id=>document.getElementById(id),map=L.map('map',{zoomControl:false}).setView([49.08,2.1],10);L.control.zoom({position:'bottomright'}).addTo(map);L.tileLayer('https://{s}.basemaps.cartocdn.com/light_nolabels/{z}/{x}/{y}{r}.png',{subdomains:'abcd',maxZoom:19,attribution:'© OpenStreetMap · © CARTO'}).addTo(map);map.createPane('noise');map.getPane('noise').style.zIndex=320;map.createPane('network');map.getPane('network').style.zIndex=430;const bounds=[[48.904,1.605],[49.248,2.595]],state={layers:{},stats:{},active:new Set(['roads','rails','aircraft','communes']),data:{}};
function openDetail(html){$('detailContent').innerHTML=html;$('detailPanel').classList.add('open')}function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function fmt(n,d=0){return Number(n||0).toLocaleString('fr-FR',{maximumFractionDigits:d})}
function noiseColor(v){return v>=70?'#691635':v>=65?'#ab202f':v>=60?'#e05b31':v>=55?'#f5a623':v>=50?'#ffe05b':v>=45?'#74c476':'#57abd2'}
const roadNoise=L.imageOverlay('data/noise-road.png',bounds,{pane:'noise',opacity:.76}),railNoise=L.imageOverlay('data/noise-rail.png',bounds,{pane:'noise',opacity:.78});state.layers.roadNoise=roadNoise;state.layers.railNoise=railNoise;
const samplers={};for(const [key,url] of [['roadNoise','data/noise-road.png'],['railNoise','data/noise-rail.png']]){const img=new Image;img.onload=()=>{const c=document.createElement('canvas');c.width=img.width;c.height=img.height;c.getContext('2d').drawImage(img,0,0);samplers[key]={canvas:c,w:img.width,h:img.height}};img.src=url}
Promise.all(['communes.geojson','commune_stats.json','roads.geojson','rails.geojson'].map(f=>fetch('data/'+f).then(r=>r.json()))).then(([communes,stats,roads,rails])=>{state.stats=stats;state.data.communes=communes;state.layers.roads=L.geoJSON(roads,{pane:'network',style:f=>({color:f.properties.type==='motorway'?'#d1495b':'#c77b30',weight:f.properties.type==='motorway'?4:2.4,opacity:.9}),onEachFeature:(f,l)=>l.bindTooltip(`<strong>${esc(f.properties.name)}</strong>`,{sticky:true}).on('click',()=>showRoad(f.properties))}).addTo(map);state.layers.rails=L.geoJSON(rails,{pane:'network',style:{color:'#5c3a8c',weight:2.5,opacity:.9,dashArray:'8 3'},onEachFeature:(f,l)=>l.bindTooltip(`<strong>${esc(f.properties.name)}</strong>`,{sticky:true}).on('click',()=>showRail(f.properties))}).addTo(map);state.layers.communes=L.geoJSON(communes,{style:{color:'#263b57',weight:.65,opacity:.55,fillOpacity:.01},onEachFeature:(f,l)=>{const code=String(f.properties.code||f.properties.insee||f.properties.INSEE_COM||'');l.bindTooltip(stats[code]?.nom||f.properties.nom,{sticky:true});l.on('click',()=>showCommune(code))}}).addTo(map);map.fitBounds(state.layers.communes.getBounds(),{padding:[16,16]});setupSearch(communes)});
function showRoad(p){openDetail(`<span class="detail-tag">AXE ROUTIER</span><h2>${esc(p.name)}</h2><div class="kpi-grid"><div class="kpi-tile"><small>Référence</small><strong>${esc(p.ref||'—')}</strong></div><div class="kpi-tile warn"><small>Catégorie</small><strong>${esc(p.type)}</strong></div></div><h3>Lecture</h3><p>Activez « Niveaux sonores routiers » pour voir les classes acoustiques modélisées autour de cet axe. La carte nocturne commence à 40 dB(A).</p><a class="profile-link" href="https://www.sytadin.fr/" target="_blank">Voir la circulation en direct ↗</a>`)}
function showRail(p){openDetail(`<span class="detail-tag">INFRASTRUCTURE FERROVIAIRE</span><h2>${esc(p.name)}</h2><div class="kpi-grid"><div class="kpi-tile"><small>Référence</small><strong>${esc(p.ref||'—')}</strong></div><div class="kpi-tile warn"><small>Type</small><strong>Voie ferrée</strong></div></div><h3>Lecture</h3><p>Activez « Niveaux sonores ferroviaires » pour afficher les secteurs exposés à partir de 55 dB(A) Lden autour du réseau.</p>`)}
function showCommune(code){const s=state.stats[code];if(!s)return;openDetail(`<span class="detail-tag">PORTRAIT COMMUNAL · 2024</span><h2>${esc(s.nom)}</h2><p><strong>${fmt(s.population)}</strong> habitants dans la statistique d’exposition.</p><div class="kpi-grid"><div class="kpi-tile warn"><small>Bruit très dégradé</small><strong>${fmt(s.bruit_degrade_pct,1)} %</strong><em>ce n’est pas « tout bruit »</em></div><div class="kpi-tile"><small>Air très dégradé</small><strong>${fmt(s.air_degrade_pct,1)} %</strong><em>ce n’est pas « toute pollution »</em></div><div class="kpi-tile warn"><small>Cumul maximal</small><strong>${fmt(s.cumul_tres_degrade_pct,1)} %</strong></div><div class="kpi-tile"><small>Population</small><strong>${fmt(s.population)}</strong></div></div><p class="flag-note">0 % signifie : aucune population dans la classe « très dégradée » concernée. Cela ne signifie jamais absence de route, de train, d’avion, de bruit ou de pollution.</p>`)}
function sampleNoise(key,latlng){const s=samplers[key];if(!s)return null;const x=Math.floor((latlng.lng-1.605)/(2.595-1.605)*s.w),y=Math.floor((49.248-latlng.lat)/(49.248-48.904)*s.h);if(x<0||y<0||x>=s.w||y>=s.h)return null;const d=s.canvas.getContext('2d').getImageData(x,y,1,1).data;if(d[3]<20)return null;const palette=[[87,171,210,40],[116,196,118,45],[255,224,91,50],[245,166,35,55],[224,91,49,60],[171,32,47,65],[105,22,53,70],[66,11,36,75]];return palette.sort((a,b)=>(a[0]-d[0])**2+(a[1]-d[1])**2+(a[2]-d[2])**2-((b[0]-d[0])**2+(b[1]-d[1])**2+(b[2]-d[2])**2))[0][3]}
map.on('click',e=>{for(const key of ['roadNoise','railNoise']){if(!state.active.has(key))continue;const v=sampleNoise(key,e.latlng);if(v){const road=key==='roadNoise';openDetail(`<span class="detail-tag">${road?'BRUIT ROUTIER':'BRUIT FERROVIAIRE'}</span><h2>${v} à ${v+5} dB(A)</h2><div class="kpi-grid"><div class="kpi-tile warn"><small>Indicateur</small><strong>${road?'Ln':'Lden'}</strong></div><div class="kpi-tile"><small>Millésime</small><strong>2017</strong></div></div><p>Classe acoustique modélisée au point cliqué. ${road?'Ln décrit la période nocturne.':'Lden agrège jour, soirée et nuit avec pondération.'}</p><p class="flag-note">Carte stratégique réglementaire : valeur moyenne modélisée, pas une mesure en direct.</p>`);break}}});
function setupSearch(geo){const items=geo.features.map(f=>{const code=String(f.properties.code||f.properties.insee||f.properties.INSEE_COM||'');return{code,name:state.stats[code]?.nom||f.properties.nom,f}}).filter(x=>x.name);function run(){const q=$('searchInput').value.trim().toLowerCase(),hits=items.filter(x=>x.name.toLowerCase().includes(q)).slice(0,8);$('searchResults').innerHTML=hits.map(x=>`<button data-code="${x.code}">${esc(x.name)}</button>`).join('');$('searchResults').hidden=!q||!hits.length;$('searchResults').querySelectorAll('button').forEach(b=>b.onclick=()=>{const x=items.find(i=>i.code===b.dataset.code);map.fitBounds(L.geoJSON(x.f).getBounds(),{maxZoom:13});showCommune(x.code);$('searchResults').hidden=true})}$('searchInput').oninput=run;$('searchButton').onclick=run}
const aircraft=L.layerGroup().addTo(map);state.layers.aircraft=aircraft;function band(ft){if(!Number.isFinite(ft))return{label:'Altitude inconnue',color:'#718096',radius:500};if(ft<3000)return{label:'Survol bas',color:'#b21f35',radius:3500};if(ft<7000)return{label:'Survol intermédiaire',color:'#db6b2f',radius:2200};if(ft<12000)return{label:'Survol élevé',color:'#d5a42d',radius:1200};return{label:'Haute altitude',color:'#66758b',radius:450}}
async function loadAircraft(){try{const r=await fetch('https://api.adsb.lol/v2/lat/49.08/lon/2.10/dist/45'),d=await r.json(),planes=(d.ac||[]).filter(p=>Number.isFinite(p.lat)&&Number.isFinite(p.lon));aircraft.clearLayers();planes.forEach(p=>{const ft=Number(p.alt_baro),b=band(ft);L.circle([p.lat,p.lon],{radius:b.radius,color:b.color,weight:1,fillOpacity:.07,interactive:false}).addTo(aircraft);L.marker([p.lat,p.lon],{icon:L.divIcon({html:`<div class="plane-marker" style="transform:rotate(${Number(p.track)||0}deg)">✈</div>`,className:'',iconSize:[24,24],iconAnchor:[12,12]})}).addTo(aircraft).bindTooltip(`${esc((p.flight||p.r||'Aéronef').trim())} · ${Number.isFinite(ft)?fmt(ft)+' ft':'altitude inconnue'}`).on('click',()=>openDetail(`<span class="detail-tag">AÉRONEF · DIRECT</span><h2>${esc((p.flight||p.r||p.hex||'Aéronef').trim())}</h2><div class="kpi-grid"><div class="kpi-tile warn"><small>Altitude</small><strong>${Number.isFinite(ft)?fmt(ft)+' ft':'—'}</strong><em>${Number.isFinite(ft)?fmt(ft*.3048)+' m':''}</em></div><div class="kpi-tile"><small>Vitesse sol</small><strong>${Number.isFinite(Number(p.gs))?fmt(Number(p.gs)*1.852)+' km/h':'—'}</strong></div><div class="kpi-tile"><small>Type</small><strong>${esc(p.t||'—')}</strong></div><div class="kpi-tile warn"><small>Lecture</small><strong>${b.label}</strong></div></div><p class="flag-note">Position ADS-B en direct. L’anneau traduit uniquement l’altitude : ce n’est pas un niveau en dB(A).</p>`))});$('aircraftMenuStatus').textContent=`${planes.length} aéronefs · actualisé ${new Date().toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}`}catch{$('aircraftMenuStatus').textContent='Flux momentanément indisponible'}}loadAircraft();setInterval(loadAircraft,60000);
function renderAir(){if(state.layers.air)map.removeLayer(state.layers.air);if(!state.active.has('air'))return;state.layers.air=L.geoJSON(state.data.communes,{style:f=>{const c=String(f.properties.code||f.properties.insee||f.properties.INSEE_COM||''),v=state.stats[c]?.air_degrade_pct||0;return{color:'#fff',weight:.7,fillColor:v>=20?'#612a4c':v>=10?'#9d597f':v>0?'#d7a9c1':'#edf0f4',fillOpacity:.78}},onEachFeature:(f,l)=>{const c=String(f.properties.code||f.properties.insee||f.properties.INSEE_COM||''),s=state.stats[c];l.bindTooltip(`${esc(s?.nom||f.properties.nom)} · ${fmt(s?.air_degrade_pct,1)} % très dégradé`,{sticky:true}).on('click',()=>showCommune(c))}}).addTo(map)}
function toggle(name,on){state.active[on?'add':'delete'](name);const layer=state.layers[name];if(name==='air')renderAir();else if(layer){if(on)layer.addTo(map);else map.removeLayer(layer)}updateLegend()}
document.querySelectorAll('.layer-card').forEach(b=>b.onclick=()=>{b.classList.toggle('active');toggle(b.dataset.layer,b.classList.contains('active'))});function updateLegend(){const parts=[];if(state.active.has('roadNoise'))parts.push('<span><i class="noise-ramp"></i>Bruit routier · Ln 40–75 dB(A)</span>');if(state.active.has('railNoise'))parts.push('<span><i class="noise-ramp"></i>Bruit ferroviaire · Lden 55–75 dB(A)</span>');if(state.active.has('roads'))parts.push('<span><i class="road-line"></i>Axes routiers</span>');if(state.active.has('rails'))parts.push('<span><i class="rail-line"></i>Voies ferrées</span>');if(state.active.has('aircraft'))parts.push('<span><i class="plane-dot"></i>Aéronefs en direct</span>');if(state.active.has('air'))parts.push('<span><i class="air-ramp"></i>Air très dégradé · population</span>');$('legendContent').innerHTML=parts.join('')||'<small>Aucune couche active</small>'}
function openSynthesis(){const pop=1221750,air=374724.29,noise=26794.99,both=17079.79;$('synthesisContent').innerHTML=`<div class="synthesis-dashboard-head"><span class="detail-tag">SYNTHÈSE DÉPARTEMENTALE</span><h2>Nuisances et expositions · Val-d’Oise</h2><p>Une vue d’ensemble des données disponibles et de leurs millésimes.</p></div><div class="synthesis-dashboard-kpis"><div class="kpi-tile"><small>Population étudiée</small><strong>${fmt(pop)}</strong><em>Airparif–Bruitparif 2024</em></div><div class="kpi-tile"><small>Air très dégradé</small><strong>${fmt(100*air/pop,1)} %</strong><em>${fmt(air)} habitants estimés</em></div><div class="kpi-tile warn"><small>Bruit très dégradé</small><strong>${fmt(100*noise/pop,1)} %</strong><em>${fmt(noise)} habitants estimés</em></div><div class="kpi-tile warn"><small>Cumul maximal</small><strong>${fmt(100*both/pop,1)} %</strong><em>${fmt(both)} habitants estimés</em></div><div class="kpi-tile"><small>Communes</small><strong>183</strong><em>géographie actuelle</em></div></div><section class="synthesis-viz"><strong>Exposition départementale</strong>${bar('Air très dégradé',100*air/pop,'#8b4b73')}${bar('Bruit très dégradé',100*noise/pop,'#d66b32')}${bar('Cumul maximal',100*both/pop,'#6b243e')}</section><section class="synthesis-viz"><strong>Sources cartographiées</strong>${bar('Routes principales',100,'#c15a37')}${bar('Réseau ferré',100,'#65448f')}${bar('Survols ADS-B',100,'#365b83')}</section><div class="synthesis-insights"><strong>À retenir</strong><p>La qualité de l’air et le bruit sont deux indicateurs différents : leurs catégories ne doivent pas être confondues.</p><p>Les cartes de bruit sont des moyennes réglementaires. Les avions sont les seuls objets suivis directement dans la carte.</p><p>Le trafic routier direct est accessible par Sytadin, sans fabriquer de correspondance acoustique.</p></div>`;$('synthesisDialog').showModal()}function bar(label,v,color){return `<div class="synthesis-bar-row"><div><span>${label}</span><b>${fmt(v,1)} %</b></div><div class="synthesis-bar-track"><i style="--bar-width:${Math.min(100,v)}%;--bar-color:${color}"></i></div></div>`}
$('clearAll').onclick=()=>{document.querySelectorAll('.layer-card.active').forEach(b=>{b.classList.remove('active');toggle(b.dataset.layer,false)});$('detailPanel').classList.remove('open')};$('resetView').onclick=()=>state.layers.communes&&map.fitBounds(state.layers.communes.getBounds(),{padding:[16,16]});$('closeDetail').onclick=()=>$('detailPanel').classList.remove('open');$('openData').onclick=$('openDataTop').onclick=openSynthesis;$('openMethod').onclick=()=>$('methodDialog').showModal();document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>$(b.dataset.close).close());$('mobileLayers').onclick=()=>$('layerSidebar').classList.toggle('open');updateLegend();
